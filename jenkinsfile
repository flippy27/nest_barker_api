pipeline{
  agent any
  stages{
    stage("veryfing stuff"){
      steps{
        sh '''
          docker version
          docker info
        '''
      }
    }
    stage("build and compose"){
      steps{
        sh 'docker compose up --wait'
        sh 'docker ps'
      }
    }
  }
}
